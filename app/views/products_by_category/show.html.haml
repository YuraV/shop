.page-header
  = link_to products_by_category_index_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back

  %h1
    Show product in
    = @category.title
    category

.table-responsive
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr
        %th Title
        %th Description
        %th Picture
        %th Price
        %th Currency
        %th
        %th
        %th

    %tbody
      - @products.each do |product|
        %tr
          %td= product.title
          %td= product.description
          %td
            =image_tag product.picture.url(:thumb)
          %td= product.price
          %td= product.currency
          %td= link_to 'Show', product
          %td= link_to 'Edit', edit_product_path(product)
          %td= link_to 'Destroy', product, data: { confirm: 'Are you sure?' }, method: :delete
