= form_for @currency, html: { class: "form-horizontal", role: "form" } do |f|
  -if @currency.errors.any?
    .alert.alert-danger.alert-dismissable{role: "alert"}
      %button.close{type: "button", data: {dismiss: "alert"} }
        %span{aria: {hidden: "true"} } &times;
        %span.sr-only Close
      %h4= "#{pluralize(@currency.errors.count, "error")} prohibited this currency from being saved:"

      %ul
        - @currency.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :iso, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :iso, class: 'form-control'
  .form-group
    = f.label :rate, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :rate, class: 'form-control'
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit class: 'btn btn-primary'
